# Cloudflare Workers — QuiltCraft stitch export
# Deploy: wrangler deploy (from /workers directory)

name = "quiltcraft"
main = "workers/export-worker.js"
compatibility_date = "2024-01-01"
account_id = "af8d4c836d48271d4dfc0f0db3c84d71"

[vars]
# Set GOOGLE_OAUTH_TOKEN in the Cloudflare dashboard (not here)
# Dashboard → Workers → quiltcraft-export → Settings → Variables

# Routes: add after Pages is live and you have a custom domain
# [[routes]]
# pattern = "quiltcraft.pages.dev/api/*"
# zone_name = "pages.dev"
