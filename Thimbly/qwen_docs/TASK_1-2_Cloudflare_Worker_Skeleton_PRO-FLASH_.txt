[PRO] Generate a Cloudflare Worker (Hono framework) for Thimbly CP1 with:

1. Core Routes:
- POST /api/auth/session: Proxy to Supabase auth, return session + user role
- GET /api/credits/balance: Return user's credit balance (RLS enforced via Supabase client)
- POST /api/exports/validate: Check credit balance, validate craft_type/format, reserve credit (idempotent)
- POST /api/exports/complete: Finalize export, deduct credit, return download URL
- POST /api/webhooks/stripe: Verify Stripe signature, update credits on successful payment

2. Security:
- JWT verification for all authenticated routes (Supabase session)
- Rate limiting via Cloudflare KV: 10 req/min per IP for export endpoints
- Idempotency enforcement: X-Idempotency-Key header required for credit operations
- Stripe webhook signature verification (use stripe-node or manual HMAC)

3. Credit Logic:
- On /validate: Check balance >=1 via Supabase RPC or direct query (with RLS)
- Reserve credit with pending status in credit_transactions (idempotency_key = request ID)
- On /complete: Update transaction to 'completed', decrement user_credits.balance
- On failure/timeout: Auto-refund via separate endpoint or cron

4. Export Pipeline Stub:
- Accept design JSON + craft_type + format (pdf/png)
- Generate watermarked preview (stub: return mock URL)
- Store in R2 bucket: thimbly-exports/{user_id}/{project_id}/{timestamp}.{format}
- Return signed R2 URL valid for 1 hour

5. Config:
- wrangler.toml with bindings: SUPABASE_URL, SUPABASE_ANON_KEY, STRIPE_WEBHOOK_SECRET, KV_NAMESPACE
- .env.example with placeholder values

Output: 
- worker/src/index.ts (Hono app with all routes)
- worker/wrangler.toml
- worker/.env.example
- worker/src/lib/supabase.ts (Supabase client init)
- worker/src/lib/stripe.ts (webhook verification helper)

Validate: 
- Auth flow returns 401 on invalid JWT
- Credit check rejects requests when balance < 1
- Webhook rejects invalid Stripe signatures
- Rate limit triggers after 10 requests in 60 seconds

[FLASH] After generating the above, create a simple test suite (worker/tests/) with:
- Jest/Vitest tests for auth, credit gating, webhook verification
- Mock Supabase/Stripe responses
- Run command: npm run test:worker

[CONTINUE] If output is truncated, respond with "##CONTINUED##" and resume exactly where you left off. Do not summarize.